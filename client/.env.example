# Base URL of the hosted FastAPI backend, e.g. https://campusos-api.onrender.com
# Leave unset for local dev (Vite proxies /api to localhost:8000)
VITE_API_BASE=

# Override the target of that dev proxy if your backend is not on 127.0.0.1:8000
# VITE_DEV_API_TARGET=http://127.0.0.1:8000
